#!/bin/bash

# 1. check the kernel version if > 4.11, Or exit 1
# 2. check if the so exist.
    # if not: copy the so from init-container to /usr/lib/xxx.
# 3. try to load the kernel module: modprobe smc


       

    